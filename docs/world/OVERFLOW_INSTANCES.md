# Overflow Instances

`overflow.ts` · template `rift_edge_outpost`

- Temporary TTL (default 6h).
- `allowsPermanentHousing = false`, zero plots, no neighborhood.
- Used for festivals / bosses / hard-cap spikes.
- Expiry archives the instance; deeds never lived here.
