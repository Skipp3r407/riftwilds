# Wallet Integration

- Wallet connect optional site-wide
- Rift Stakes lobby may show wallet for deposit DEMO; practice `/tcg/battle` never requires it
- On-chain mode needs `SOL_WALLET_ENABLED` + `RIFT_STAKES_ONCHAIN_ENABLED`
- DEMO escrow does not broadcast transactions
