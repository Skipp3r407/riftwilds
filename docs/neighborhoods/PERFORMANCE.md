# Neighborhood Performance

- Exteriors stream with Live World map chunks (Commons first)
- LOD / occlusion / interest management stubs shared with housing `performance.ts`
- Private interiors never simulate other players' furniture sets
- NPC life counts are lightweight aggregates (musicians/animals/visitors), not full AI sims per plot
- Community project completion adds landmarks (capped list) rather than unbounded props
