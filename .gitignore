# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local
.env.production
.env.development

# Nakama local runtime scratch (compose volume mount may write here)
/nakama/data/
!/nakama/modules/.gitkeep

# local treasury ops ledger (demo persistence)
/.data/

# user-uploaded feedback screenshots (keep folder via .gitkeep)
/public/uploads/feedback/**
!/public/uploads/feedback/.gitkeep

# local scratch / agent temp
/tmp-*.json
/artifacts/**/*.log
/artifacts/prod-server*.log
/.cursor/
/.cache/
/public/assets/brand/_pre_enhance/

# Third-party discovery packs (raw zips / unrestricted mirrors) — never ship in public/
/private-assets/discovered/*
!/private-assets/discovered/.gitkeep
/private-assets/rejected/*
!/private-assets/rejected/.gitkeep
/private-assets/restricted/*
!/private-assets/restricted/.gitkeep
# approved/ may track small cleared sources later; still prefer git-lfs / local for large zips

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/*.db
/prisma/*.db-journal

/tmp-*.txt

