# Fonts for Issue #002 lettering

Lettering uses `src/lib/comics/lettering-engine.ts`.

Fonts must **not** live under `/public`. Prefer project-private font files under this issue tree or shared comics font cache outside the web root (same rule as Issue #001).
